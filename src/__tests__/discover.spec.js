import { mount } from '@vue/test-utils';
import Discover from 'src/views/Discover.vue';

describe('Discover', () => {
    it('shows correct image in slides', () => {
        expect(typeof App.data).toBe('function');
    });
});